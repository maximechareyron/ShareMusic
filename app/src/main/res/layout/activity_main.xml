<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/bg_register"
    tools:context="com.example.olmartin2.lecteurmusique.MainActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/boutton_main_transmitter"
        android:id="@+id/emetteur"
        android:layout_above="@+id/hebergeur"
        android:layout_centerHorizontal="true"
        android:background="@color/bg_login"
        android:layout_marginBottom="75dp"
        android:width="250dp"
        android:layout_centerVertical="true"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_main_host"
        android:id="@+id/hebergeur"
        android:background="@color/bg_login"
        android:layout_centerVertical="true"
        android:layout_alignRight="@+id/emetteur"
        android:layout_alignEnd="@+id/emetteur"
        android:width="250dp"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/manage_acc"
        android:id="@+id/manage_acc"
        android:background="@color/bg_login"
        android:layout_centerHorizontal="true"
        android:width="250dp"
        android:layout_centerVertical="true"
        android:layout_above="@+id/emetteur"
        android:layout_marginBottom="75dp"
        />

</RelativeLayout>
